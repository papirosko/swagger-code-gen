<%_ const sanitizeDocLine = (line) => String(line).replace(/\*\//g, '*\\/');
const renderDocLines = (text) => String(text).split(/\r?\n/).map(line => ` * ${sanitizeDocLine(line)}`); -%>
/**
 * <%= method.method.toUpperCase() %> <%= method.path %>
<% if (method.summary) { -%>
<%_ renderDocLines(`@summary ${method.summary}`).forEach(line => { -%>
<%= line %>
<%_ }); -%>
<% } -%>
<% if (method.description) { -%>
<%_ renderDocLines(`@description ${method.description}`).forEach(line => { -%>
<%= line %>
<%_ }); -%>
<% } -%>
<% if (method.wrapParamsInObject) { -%>
 * @param {Object} params
<% } -%>
<% method.parameters.foreach(p => { -%>
<%_ renderDocLines(`@param {${p.jsType}} ${method.wrapParamsInObject ? 'params.' : ''}${p.uniqueName} ${p.description.getOrElseValue('')}`).forEach(line => { -%>
<%= line %>
<%_ }); -%>
<% }); -%>
<%_ if (body.nonEmpty) { -%>
<%_ renderDocLines(`@param {${body.get.body.jsType}} body ${method.bodyDescription.getOrElseValue('')}`).forEach(line => { -%>
<%= line %>
<%_ }); -%>
<%_ } -%>
 * @param {RequestOptions} requestOptions Additional request params
 * @return {<%= method.response.noContent ? 'void' : (method.response.parseMode === 'bytes' ? (targetNode ? 'Buffer' : 'ArrayBuffer') : method.response.responseType) %>} <%= method.response.description %>
 */
export async function <%= method.endpointName %><%= body.map(b => b.suffix).getOrElseValue('') %>(
    <%_ if (!method.wrapParamsInObject) { -%>
    <%_ method.parameters.foreach(p => { -%>
    <%= p.uniqueName %><%= p.required ? '' : '?'%>: <%- p.jsType %><%- p.defaultValue.map(x => ` = ${x}`).getOrElseValue('') %>,
    <%_ }) -%>
    <%_ } else { -%>
    params: {
        <%_ method.parameters.foreach(p => { -%>
        <%= p.uniqueName %><%= p.required ? '' : '?'%>: <%- p.jsType %>,
        <%_ }) -%>
    },
    <%_ } -%>
    <%_ if (body.nonEmpty) { -%>
    body<%= body.get.body.required ? '' : '?'%>: <%- body.get.body.jsType %>,
    <%_ } -%>
    requestOptions: RequestOptions = defReqOpts()
): Promise<<% if (method.response.noContent) { %>void<% } else if (method.response.parseMode === 'bytes') { %><% if (targetNode) { %>Buffer<% } else { %>ArrayBuffer<% } %><% } else { %><%- method.response.responseType %><% } %>> {
    let query = '';
    <%_ if (method.parameters.filter(x => x.in === 'query').nonEmpty) { -%>
    const queryParams = [];
    <%_ method.parameters.filter(x => x.in === 'query' && x.required).foreach(p => { -%>
    <%_ if (p.isArray) { -%>
    <%= method.wrapParamsInObject ? 'params.' : '' %><%= p.uniqueName %>.forEach(p => {
        queryParams.push(`<%= p.rawName %>=${encodeParamValue(p)}`);
    });
    <%_ } else { -%>
    queryParams.push(`<%= p.rawName %>=${encodeParamValue(<%= method.wrapParamsInObject ? 'params.' : '' %><%= p.uniqueName %>)}`);
    <%_ }  -%>
    <%_ }) -%>
    <%_ method.parameters.filter(x => x.in === 'query' && !x.required).foreach(p => { -%>
    if (<%= method.wrapParamsInObject ? 'params.' : '' %><%= p.uniqueName %> !== undefined) {
        <%_ if (p.isArray) { -%>
        <%= method.wrapParamsInObject ? 'params.' : '' %><%= p.uniqueName %>.forEach(p => {
            queryParams.push(`<%= p.rawName %>=${encodeParamValue(p)}`);
        });
        <%_ } else { -%>
        queryParams.push(`<%= p.rawName %>=${encodeParamValue(<%= method.wrapParamsInObject ? 'params.' : '' %><%= p.uniqueName %>)}`);
        <%_ }  -%>
    }
    <%_ }) -%>
    if (queryParams.length > 0) {
        query = '?' + queryParams.join('&');
    }
    <%_ }  -%>

    <%_ if (method.parameters.filter(x => x.in === 'cookie').nonEmpty) { -%>
    const cookieParams = [];
    <%_ method.parameters.filter(x => x.in === 'cookie' && x.required).foreach(p => { -%>
    <%_ if (p.isArray) { -%>
    <%= method.wrapParamsInObject ? 'params.' : '' %><%= p.uniqueName %>.forEach(p => {
        cookieParams.push(`<%= p.rawName %>=${encodeParamValue(p)}`);
    });
    <%_ } else { -%>
    cookieParams.push(`<%= p.rawName %>=${encodeParamValue(<%= method.wrapParamsInObject ? 'params.' : '' %><%= p.uniqueName %>)}`);
    <%_ }  -%>
    <%_ }) -%>
    <%_ method.parameters.filter(x => x.in === 'cookie' && !x.required).foreach(p => { -%>
    if (<%= method.wrapParamsInObject ? 'params.' : '' %><%= p.uniqueName %> !== undefined) {
        <%_ if (p.isArray) { -%>
        <%= method.wrapParamsInObject ? 'params.' : '' %><%= p.uniqueName %>.forEach(p => {
            cookieParams.push(`<%= p.rawName %>=${encodeParamValue(p)}`);
        });
        <%_ } else { -%>
        cookieParams.push(`<%= p.rawName %>=${encodeParamValue(<%= method.wrapParamsInObject ? 'params.' : '' %><%= p.uniqueName %>)}`);
        <%_ }  -%>
    }
    <%_ }) -%>
    <%_ }  -%>

    let bodySerialised: BodyInit | null = null;
    <%_ if (body.nonEmpty && body.get.mimeType === 'application/json') {%>
    bodySerialised = body != null ? JSON.stringify(body) : null;
    <%_ } else if (body.nonEmpty && body.get.mimeType === 'application/x-www-form-urlencoded') { -%>
    bodySerialised = body != null ? objectToFormWwwEncoded(body) : null;
    <%_ } else if (body.nonEmpty && body.get.mimeType === 'multipart/form-data') { -%>
    bodySerialised = body != null ? objectToForm(body) : null;
    <%_ } else if (body.nonEmpty && body.get.mimeType === 'application/octet-stream') { -%>
    bodySerialised = body ?? null;
    <%_ }  -%>

    const headers: Record<string, string> = {
        ...requestOptions.headers || {},
        'Accept': '<%= method.response.mimeType %>',
        <%_ if (body.nonEmpty && body.get.mimeType !== 'multipart/form-data') {%>
        'Content-Type': '<%= body.get.mimeType %>',
        <%_ }  -%>
        <%_ if (method.parameters.filter(x => x.in === 'header').nonEmpty) { -%>
        <%_ method.parameters.filter(x => x.in === 'header').foreach(p => { -%>
        <%_ if (p.required) { -%>
        '<%= p.rawName %>': <%= method.wrapParamsInObject ? 'params.' : '' %><%= p.uniqueName %>,
        <%_ } else { -%>
        ...(<%= method.wrapParamsInObject ? 'params.' : '' %><%= p.uniqueName %> != null ? {'<%= p.rawName %>': <%= method.wrapParamsInObject ? 'params.' : '' %><%= p.uniqueName %>} : {}),
        <%_ } -%>
        <%_ }) -%>
        <%_ }  -%>
    };
    <%_ if (body.nonEmpty && body.get.mimeType === 'multipart/form-data') {%>
    delete headers['Content-Type'];
    <%_ }  -%>
    <%_ if (method.parameters.filter(x => x.in === 'cookie').nonEmpty) { -%>
    if (cookieParams.length > 0) {
        headers['Cookie'] = headers['Cookie'] ? `${headers['Cookie']}; ${cookieParams.join('; ')}` : cookieParams.join('; ');
    }
    <%_ }  -%>


    const request = new Request(`${requestOptions.apiPrefix}<%- method.pathWithSubstitutions %>${query}`, {
        method: '<%= method.method %>',
        body: bodySerialised,
        signal: requestOptions.signal,
        headers: headers
    });
    return requestImpl<<% if (method.response.noContent) { %>void<% } else if (method.response.parseMode === 'bytes') { %><% if (targetNode) { %>Buffer<% } else { %>ArrayBuffer<% } %><% } else { %><%- method.response.responseType %><% } %>>(request, requestOptions, '<%= method.response.parseMode %>');
}
